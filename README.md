🚀 YOLOv8 Object Detection Trainingโปรเจกต์นี้สำหรับฝึกสอนโมเดล (Training) YOLOv8 เพื่อใช้ตรวจจับวัตถุ (Object Detection) โดยใช้ dataset ของคุณเอง สคริปต์นี้ถูกเขียนให้รองรับการทำงานบน GPU (CUDA) เพื่อความรวดเร็ว📋 สิ่งที่ต้องเตรียม (Prerequisites)ติดตั้ง Library ที่จำเป็นก่อนเริ่มใช้งาน:pip install ultralytics torch
📂 การเตรียมข้อมูล (Dataset Setup)ตรวจสอบให้แน่ใจว่าโฟลเดอร์ Dataset ของคุณมีการจัดเรียงตามรูปแบบ YOLO ดังนี้:dataset/
├── data.yaml       # ไฟล์ config หลัก
├── train/
│   ├── images/     # รูปภาพสำหรับ train
│   └── labels/     # ไฟล์ .txt annotation
└── val/
    ├── images/     # รูปภาพสำหรับ validate
    └── labels/     # ไฟล์ .txt annotation
ตัวอย่างเนื้อหาในไฟล์ data.yaml:path: /content/dataset  # path หลัก
train: train/images
val: val/images
names:
  0: person
  1: bicycle
  # ใส่ชื่อคลาสของคุณที่นี่
🛠️ วิธีใช้งาน (Usage)แก้ไข path ในโค้ดส่วน dataset_path ให้ตรงกับตำแหน่งโฟลเดอร์ของคุณรันสคริปต์เพื่อเริ่มเทรน:# การตั้งค่าการเทรน (Training Config)
model.train(
    data='dataset/data.yaml',
    epochs=50,
    batch=16,
    imgsz=640,
    device=0,  # ใช้ 0 สำหรับ GPU หรือ 'cpu'
    name='my_custom_yolo_model'
)
📊 ผลลัพธ์ (Results)หลังจากเทรนเสร็จสิ้น ไฟล์โมเดลและกราฟผลลัพธ์จะถูกบันทึกไว้ที่:runs/detect/my_custom_yolo_model/Best Model: weights/best.pt (นำไฟล์นี้ไปใช้งานจริง)Last Model: weights/last.pt📝 หมายเหตุสคริปต์จะตรวจสอบ GPU ให้อัตโนมัติ ถ้ามี GPU จะทำงานได้เร็วกว่า CPU มากสามารถเปลี่ยนขนาดโมเดลได้ที่บรรทัด model = YOLO('yolov8n.pt') (เช่น yolov8s.pt, yolov8m.pt)Powered by Ultralytics YOLOv8
